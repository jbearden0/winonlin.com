# WinOnLin Windows App

This app runs on Windows and:
 * Presents Linux apps as a directory under the Start Menu application options.
 * Launches and displays applications via XPRA using GL acceleration.
 * Must be running before the windows instance can be toggled as a window manager option.
 * Is built using a very mature .Net version so it can be used on very mature windows versions.
